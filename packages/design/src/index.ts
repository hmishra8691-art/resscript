/**
 * @resscript/design — reserved for milestone P4-01.
 *
 * MaxDiff and conjoint experimental design generation. Deliverable C 10.
 *
 * This placeholder exists so `tsc -b` can build the workspace project graph before the
 * package has content: TypeScript treats a composite project with no input files as error
 * TS18003, which would break the root build for every other package. Delete it when P4-01
 * lands.
 */
export const MILESTONE = 'P4-01' as const;
