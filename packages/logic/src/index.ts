/**
 * @resscript/logic — reserved for milestone P1-06.
 *
 * AST, three-valued logic, type checker, dependency graph, evaluator. Deliverable D.
 *
 * This placeholder exists so `tsc -b` can build the workspace project graph before the
 * package has content: TypeScript treats a composite project with no input files as error
 * TS18003, which would break the root build for every other package. Delete it when P1-06
 * lands.
 */
export const MILESTONE = 'P1-06' as const;
