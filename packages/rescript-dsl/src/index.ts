/**
 * @resscript/rescript-dsl — reserved for milestone P1-07.
 *
 * Lexer, parser, resolver, pretty-printer. Deliverable D 6.
 *
 * This placeholder exists so `tsc -b` can build the workspace project graph before the
 * package has content: TypeScript treats a composite project with no input files as error
 * TS18003, which would break the root build for every other package. Delete it when P1-07
 * lands.
 */
export const MILESTONE = 'P1-07' as const;
